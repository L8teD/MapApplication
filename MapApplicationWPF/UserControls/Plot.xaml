<UserControl x:Class="MapApplicationWPF.UserControls.Plot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MapApplicationWPF.UserControls"
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <oxy:PlotView Model="{Binding MyPlotModel}" Controller="{Binding MyPlotController}">
        </oxy:PlotView>
        <Button x:Name="btn_Home" HorizontalAlignment="Right" VerticalAlignment="Top" Height="30" Width="30" Click="btn_Home_Click">
            <Image Source="pack://application:,,,/AnotherFiles/Images/dog-house.png" Stretch="Fill"></Image>
        </Button>
    </Grid>
</UserControl>
